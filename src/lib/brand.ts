// Single source of truth for the platform brand name shown in the dashboard UI.
// NOTE: this is the TICKETING PLATFORM brand only. The "Keshless Wallet" payment
// method label is intentionally NOT derived from here — Keshless is a payment rail
// we still accept, not the platform brand.
export const BRAND_NAME = 'Carrot Tickets';
export const SUPPORT_EMAIL = 'support@carrottickets.com';
